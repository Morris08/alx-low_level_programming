#include "main.h"
#include <stdio.h>

/**
 * _islower - Entry point
 *
 * Return: Always 0 (Success)
 *
 * @c: The character in ASCII code
 */

int _islower(int c)
{
	p = 'c';

	if (p == 'c')
	{
	_putchar(1);
	}
	if (p != 'c')
	{
	_putchar(0);
	}
return (0);
}
