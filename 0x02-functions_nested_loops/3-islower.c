#include "main.h"
#include <stdio.h>

/**
 * _islower - Entry point
 *
 * Return: Always 0 (Success)
 *
 * @c: The character in ASCII code
 */

int _islower(int c)
{
	c;

	if (p == 'c')
	{
	_puctchar(1);
	}
	if (p != 'c')
	{
	_putchar(0);
	}
return (0);
}
