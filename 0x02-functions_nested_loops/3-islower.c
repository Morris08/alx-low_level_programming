#include "main.h"
#include <stdio.h>

/**
 * _islower - Entry point
 *
 * Return: Always 0 (Success)
 *
 * @c: The character in ASCII code
 */

int _islower(int c)
{
	c = 'c';

	if (c == 'c')
	{
	_putchar (1);
	}
	if (c != 'c')
	{
	_putchar (0);
	}
	_putchar('\n');
return (0);
}
