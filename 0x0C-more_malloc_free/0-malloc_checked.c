#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * *malloc_checked - check process
 * @b: integer
 * Return: pointer
 */

void *malloc_checked(unsigned int b)
{
	void *ptr;

	ptr = malloc(b);

	if (p == NULL)
	{
	exit(98);
	}
	return (ptr);
}
