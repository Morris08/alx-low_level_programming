#include <stdio.h>
/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i, j, result;

	if (argc < 0)
	{
	printf("Error \n");
	}
	else
	{
	result = i * j;
	printf("%d\n", result);
	}
	return (1);
}
