#include <stdio.h>
/**
 * main - entry
 * @argc: count
 * @argv: vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argv = argv;
	int i, j, mul;

	mul = i * j;
	if (argc <= 2)
	{
	printf("Error\n");
	return (1);
	}
	else
	printf("%d\n", mul);
	return (0);
}
