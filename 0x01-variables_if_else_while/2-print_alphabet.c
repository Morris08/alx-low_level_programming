#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (char current_char = 'a'; current_char <= 'z'; current_char++);
	{
		putchar(current_char);
	}
	puchar('\n');

return (0);
}
