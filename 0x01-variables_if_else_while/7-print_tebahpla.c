#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	char current_char = 'z';
{
	while (current_char <= 'a')
		putchar(current_char--);
}

	putchar('\n');

return (0);
}
